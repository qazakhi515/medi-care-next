## Hospital Detail Page Fix

Before editing, read `AGENTS.md`.

Task: Fix only the hospital detail page opened from `/hospitals/:id`.

Requirements:

- Do not change any unrelated pages, layout, API, routes, styles, or business logic.
- Keep existing design and components as much as possible.
- Fix broken image layout and misaligned icons only inside the hospital detail page.
- Add a short hospital introduction section.
- Add a hospital image gallery using uploaded hospital images.
- Gallery must use Framer Motion.
- Gallery should automatically slide horizontally every 4 seconds.
- Gallery should also support mobile swipe/drag.
- Add a short medical equipment introduction section.
- Use responsive, clean, professional hospital UI.
- Use dynamic hospital data only; do not hardcode hospital-specific content.
- Make minimal changes and avoid unnecessary refactoring.

Sections order:

1. Hospital Introduction
2. Hospital Gallery
3. Medical Equipment Introduction

Implementation mode:

- Token-efficient.
- Smallest safe code changes.
- Reuse existing components.
- Preserve all current functionality.
