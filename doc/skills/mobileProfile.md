Create a minimal responsive My Page for logged-in users.

Requirements:

- Replace placeholder My Page with real UI.
- Keep desktop unchanged.
- Reuse existing auth/user data.
- Show: profile info, quick actions, upcoming appointments, favorites, settings, logout.
- Mobile: cards stack vertically, buttons fit screen, no overflow.
- Use existing styles/classes as much as possible.
- Add @media (max-width: 768px).
- Do not add fake API logic.
- Run npm run build and fix errors.
