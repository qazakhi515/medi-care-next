Now make Hospital Detail page responsive.

File:

- detail.tsx

Current problem:

- Mobile version is only a placeholder: <div>HOSPITAL DETAIL PAGE</div>
- Replace this placeholder with the real Hospital Detail UI adapted for mobile.

Requirements:

- Keep desktop version unchanged.
- Do not modify other pages.
- Use the existing Hospital Detail desktop layout and data.
- On mobile, hospital image, info, doctors, reviews, appointment buttons, and detail sections must fit screen width.
- Cards/sections must stack vertically on mobile.
- Buttons should be full-width or properly aligned.
- Images must use width: 100% and max-width: 100%.
- Text size and padding should be adjusted for mobile.
- No horizontal overflow.
- Add responsive CSS using @media (max-width: 768px).
- Run npm run build and fix errors if they appear.
